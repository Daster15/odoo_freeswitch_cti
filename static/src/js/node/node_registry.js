/** @odoo-module **/

import { registry } from "@web/core/registry";

// Create a new category in the registry for odoo_freeswitch_cti nodes
const NodeRegistry = registry.category("odoo_freeswitch_cti_nodes");

export default NodeRegistry;
